#include <stdio.h>

int main(){
    printf("<< Pointers >>\n");

    return 0;
}
/*
Cada tipo de variavel tem um tamanho unico de alocação ( int = 4; double  = 8; char = 1...); por isso que
quando deslocamos o ponteiro mesmo em valores iguais (++ ou --), o numero de bytes deslocados será diferente

*/
